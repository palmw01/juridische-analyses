name: CI — project-validatie

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Python 3.11
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements.lock

      - name: Installeer dependencies
        run: pip install -r requirements.lock

      - name: Full CI (validate + export-rdf + export-graph + check-enrichment)
        run: make ci
